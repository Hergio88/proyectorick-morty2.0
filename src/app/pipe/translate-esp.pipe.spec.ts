import { TranslateEspPipe } from './translate-esp.pipe';

describe('TranslateEspPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateEspPipe();
    expect(pipe).toBeTruthy();
  });
});
